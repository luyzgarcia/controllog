<%= form_for(@tipo_pagamento, :html => { :role => "form", :class =>"form-horizontal"}) do |f| %>
  <% if @tipo_pagamento.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipo_pagamento.errors.count, "error") %> prohibited this tipo_pagamento from being saved:</h2>

      <ul>
      <% @tipo_pagamento.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  
  	<div class="form-wrapper-group">
		<div class="form-group">
			<%= f.label :nome, 'Nome', :class => "col-sm-1 control-label"%>
			<div class="col-sm-2">
			<%= f.text_field :nome, :class =>"form-control" %>
			</div>			
	  	</div>
	</div>
	
	<div class="form-wrapper-group">
		<div class="form-group">
			<%= f.label :status, 'Status', :class => "col-sm-1 control-label"%>
			<div class="col-sm-2">
			<%= f.select(:status, options_for_select([['Ativo',1], ['Inativo',0]]),{ }, {:class =>"form-control"})  %>
			</div>			
	  	</div>
	</div>
  <div class="actions">
    <%= f.submit 'Salvar', :class => "btn btn-success" %>
    <%= link_to 'Cancelar', tipo_pagamentos_path,  :class => "btn btn-danger" %>
  </div>
<% end %>
